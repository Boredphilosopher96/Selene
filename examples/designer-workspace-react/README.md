# Designer workspace React sample

This source is the portable sample referenced by the first workspace slice. It
uses React components, routes, visual state, and stable `data-selene-node-id`
markers; it is not an HTML-string renderer.
