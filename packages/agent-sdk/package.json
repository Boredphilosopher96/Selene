{
  "name": "@selene/agent-sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:public-api": "bun run build && tsc -p tsconfig.public-api.json && node scripts/assert-public-api.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
