import { Button } from '../../packages/ui/src/index';

/** A minimal browser consumer used to guard the primitive package boundary. */
export const PrimitiveConsumer = () => <Button>Save changes</Button>;
